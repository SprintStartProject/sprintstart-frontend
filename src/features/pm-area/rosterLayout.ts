/**
 * The team roster's columns from `md` up — member, where they are, what is open with the
 * manager, progress, actions. One string so the header row and every member row line up.
 */
export const ROSTER_COLUMNS =
  "md:grid-cols-[minmax(0,1fr)_minmax(0,1.05fr)_minmax(0,1.35fr)_8rem_4.5rem]";
